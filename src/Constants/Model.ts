export enum Post {
    Status = 29,
    Picture = 26,
    Video = 28,
    Album = 27,
}

export enum Comment {
  Comment = 30,
  CommentPicture = 31
}

export enum Upload {
  CommentImage = "CommentImage"
}
